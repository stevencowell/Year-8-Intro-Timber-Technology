const key='year8-footstool-folio-v1';const state=JSON.parse(localStorage.getItem(key)||'{}');for(const [id,field] of [['student-name','name'],['student-class','class']]){const el=document.getElementById(id);el.value=state[field]||'';el.addEventListener('input',()=>{state[field]=el.value;localStorage.setItem(key,JSON.stringify(state));});}document.getElementById('print-folio').addEventListener('click',()=>window.print());
